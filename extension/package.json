{
  "name": "kings-track-gradeo-extension",
  "private": true,
  "version": "0.1.0",
  "description": "Firefox/Chrome extension for importing Gradeo data into Kings Track",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "pretest": "npm run build",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.4",
    "@types/jsdom": "^21.1.7",
    "esbuild": "^0.25.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.5.3"
  }
}
