"""AI chat assistant package.

Provides a Google Gemini-backed conversational assistant that can read
analytics data (students, courses, tasks, attendance) through a set of
read-only "data tools" exposed to the model via function calling.
"""
